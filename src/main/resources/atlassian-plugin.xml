<atlassian-plugin key="${project.groupId}.${project.artifactId}" name="${project.name}" plugins-version="2">
    <plugin-info>
        <description>${project.description}</description>
        <version>${project.version}</version>
        <vendor name="${project.organization.name}" url="${project.organization.url}" />
    </plugin-info>
    <web-resource key="notloggedin" name="notloggedin Resources">
        <dependency>jira.webresources:global-static</dependency>
        <condition class="com.corefiling.jira.plugin.notloggedin.IsNotLoggedInCondition"/>
        <resource type="download" name="login.js" location="/js/login.js"/>
        <context>jira.view.issue</context>
        <context>jira.create.issue</context>
  </web-resource>
</atlassian-plugin>